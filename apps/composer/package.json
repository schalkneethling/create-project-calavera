{
  "name": "@calavera/composer",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "semver": "7.8.5"
  },
  "devDependencies": {
    "ajv": "8.20.0",
    "vite": "^8.2.0"
  }
}
